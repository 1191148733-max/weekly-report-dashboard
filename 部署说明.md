# 周报看板 Netlify 部署包

本包为静态站点。周报数据、筛选交互和店铺明细均已内嵌在 `index.html` 中，发布时不上传原始 Excel、Python 脚本或本地数据目录。

## 一键更新

双击桌面的 `/Users/yangwu/Desktop/周报数据更新.command`。入口会依次刷新周报数据、生成部署包、同步 GitHub，并等待 Netlify 发布完成。

## 手动部署备用方式

可将桌面的 `周报看板_Netlify部署包.zip` 上传至 Netlify，或直接拖入本文件夹。

`netlify.toml` 已关闭页面缓存，重新部署后可读取最新周报版本。
